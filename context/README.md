# context/

Internal development documentation — the reasoning behind the stack, kept
separate from `README.md`, which is the operating manual.

| File | What it holds |
| --- | --- |
| `HANDOFF.md` | The state of play at the end of a working session: every path touched, what changed and why it mattered, what is verified against a real run and what is not, and the brief for the next session. Newest entry first. The top entry is **AO7 finished, and a scan that could not fail** — §AI.7 was run at last, both columns, against the live stack: `MARKER-TOKEN-9F42-RELOCATED` against *(Skill "relocated-marker" not found…)*, the first time anything about AO7 has been observed rather than asserted. Its **third** recipe was the first that could fail, and the two control runs are the real finding: putting root 3 back to the hardcoded join fails 2 of 516 tests, but leaving the `agentDir()` call exactly as shipped and setting `includeDefaults: false` — root 3 gone, the right value computed and discarded — leaves **all 516 green**, because every assertion holding AO7 read the source as text. **A test that reads the source cannot tell a call from a use.** Probe `ab11` is the behavioural half, and it showed what the hand test could not: with a skill of the same name left in the old directory, the defect does not report a miss, it hands the child **the old file, silently**. 1,447 tests unchanged, 123 → **124** probes, 35 → **40** `ab*` modes. Below it, **AO10 and three controls that could not fail** — trying to run the last cheap hand test found that `scripts/pi-local.sh` asked where pi's agent directory is in four places and answered two ways, so a relocated install got **no model provider at all**; and the hand test itself then corrected the finding it was testing, because a child's ordinary skill discovery goes through pi's own `getAgentDir()` and never reaches the code AO7 fixed. The sentence to carry: **a control that produces the same answer as the fixed column is not a passing test, it is a broken instrument.** 1,441 → **1,447** tests, 122 → **123** probes. Below it, the **hand-test session**: the twenty-fourth pass's cheapest unrun hand test finally run against the live stack — `AgentStatus` printed eight characters, `StopAgent` accepted them — and the control run that BEFORE column needed found **AO9**, which is that AO1's fix was held by nothing: putting `tool-execution.ts:450` back to its pre-AO1 `getRecord(requestedId)` left **1,434 tests and all 121 probes green**, while one live delegation caught it on the first call. The reason `ab1` quoted the old expression instead of driving the function was **the suite's** constraint, not a probe's — `q2` has driven that same function through pi's own jiti since the thirteenth pass — so AO9 got both a probe (`ab9`, all four modes exit 1 with the defect restored) and a source pin. Also: **§AI of the hand-testing script, written because two documents referenced it in three places and it did not exist.** 1,434 → **1,441** tests, lint 115/115, 121 → **122** probes. Below it, the **write-up session** for the twenty-fourth pass: the document written, the propagation done, the probe count settled, AO5's operator-facing half seen firing against a real hand-edit for the first time, and an eighth finding — **AO8** — which is that pass's own recorded decision being reversed within the hour, because the reason it gave for leaving a latent alone ("the case is not reachable on this box") took four minutes with real git to disprove. 1,424 → **1,434** tests, lint 115/115, 120 → **121** probes. Below it, the **twenty-fourth** pass itself over subagents/loop/verifier — seven findings plus an eighth closed the same day, all fixed, nearly every one a place where what the stack PUBLISHES is not what its own lookup ACCEPTS. The headline is the agent id: four model-facing surfaces print the first eight characters of a seventeen-character id and `StopAgent` resolved it with an exact `Map.get`, so it refused every identifier the model had ever been shown — and its own refusal handed back more of them. Two are live on this box: the prinny suite was green against a staged build whose sources no longer exist in the tree, and `--prepare` (carried as never-exercised and possibly broken) works here. 1,369 → 1,424 → 1,434 tests, 113 → 121 probes. Written up in full in `design/subagents-loop-verifier-identity.md`. Below it, the entry rewritten 2026-08-23 after the **twenty-third** pass over subagents/loop/verifier — seven findings, all fixed, every one of them a value written in one place and read back in another by a question that did not match. The headline is one `catch` returning `{}` for both "there is no file" and "nobody could read this file", in two config readers whose next write replaces the file — and the prinny instance of it takes `permissionMode` from `all` to `off` and then saves that. Two findings are live on this box: the staged sidecar runtime is a build from before AL3 that three readers called "built", and 41% of a real session file is loop state with two of every five entries byte-identical to the one before. 1,281 → 1,369 tests, 111 → 118 probes. Below it, the entry for the **twenty-second** pass over subagents/loop/verifier — six findings, all fixed, every one of them two points in the code that are each correct and wrong about the time between them. The headline closes a bound the handoff had carried as open for seven passes: the compaction lock could only be read for compactions an *extension* asked for, and the sentence that kept it open named the wrong event — `session_before_compact` is an `ExtensionEvent` and pi emits it for every compaction it runs. 1,222 → 1,281 tests, 106 → 111 probes. Below it, the entry for the **twenty-first** pass over subagents/loop/verifier — nine findings, all fixed, every one of them a construct with a beginning whose end is missing, unreachable, or reachable by one path out of N. The headline is the prinny sidecar's connect loop, which retries a homeserver forever and built a Matrix client per attempt with nothing anywhere stopping one — on one Olm crypto store, in a tree whose own `state.ts` header says that store must never be shared between two running bots. It also carries a warning that is not a finding: a cleanup line in the first draft of this pass's own spill test resolved to `/tmp` and deleted it. 1,174 → 1,222 tests, 97 → 106 probes. Below it, the entry for the **twentieth** pass over subagents/loop/verifier — five findings, all fixed, every one of them a predicate whose NAME is a property and whose TEST is a proxy for it; plus the transcript work the nineteenth pass recorded as asked for and not done, now built and measured. The headline is the loop's audit rung, which could not fire on the runs it was written for because `42 passed` counted as a change to the project. 1,108 → 1,153 tests, 87 → 93 probes, lint 97/97. Below it, the entry for the **nineteenth** pass over subagents/loop/verifier — five findings, all fixed, every one of them a guard that names an ACTOR, is correct about that actor, and is reached by a different one. The stack has five: the operator at the terminal, the parent model, an allow-listed Matrix sender, a child session in this process, and the machinery itself. The headline is `/stack` — advertised to a Matrix client as “Show local model stack status”, allowed in full, and every branch of it ends in `pi.exec`, which no `tool_call` handler ever sees. 1,041 → 1,071 → 1,108 tests, 82 → 87 probes, lint 95/95. It also carries one thing that is NOT a finding: an operator request for a subagent's own turns to be written into the session transcript, marked as a subagent's, with the mechanism (`pi.appendEntry`, which pi never turns into a context message) and the three constraints already measured — that is the next session's item 0. Below it, the entry for the **eighteenth** pass over subagents/loop/verifier — six findings, all fixed, every one of them a rule this stack has already paid for being applied to fewer places than need it. The headline is the sixteenth pass's own residue answered: `compactionInFlight()` had a fifth reader that already existed, on the one delivery path with no second attempt, and it closes §11.11 (`UNADDRESSED` read as ADDRESSED) which had been open since the twelfth. 1,018 → 1,041 tests, 72 → 77 probes, lint 91/91 → 95/95. Below it, the entry for the **sixteenth** pass over subagents/loop/verifier — six findings, all fixed, five of them a pointer that was never followed (a flag, a tool, an entry point, a sibling function's own rule), and in every one the thing pointed at already existed and already worked. Two are the same moment from two extensions: pi's "cannot prompt while compacting" refusal lives on `prompt()` alone, and both the loop's next iteration and prinny's empty-turn continuation were being delivered into a compaction that was already running, while `compactionInFlight()` — the lock the fifteenth pass built — said who was compacting. 991 → 1,018 tests, 67 → 72 probes, lint clean. Below it, the entry for the **fifteenth** pass — six findings, all fixed, two of them a previous pass's own reasoning one step out and two of them a bound that kept the wrong end — plus three open items closed after them, including the fourteenth pass's own homework (two extensions compacting one session) and the judge's raw reply, which had been #1 by age for twelve passes. Each fix carries a regression test that fails when it is removed. |
| `design/subagents-loop-verifier-identity.md` | **The current account of subagents, the loop and the verifier, and the one written to be read on its own.** The twenty-fourth pass. **§11.10 (AO9) is the one to read before trusting any control-run number in this series** — added 2026-08-23, it is this document's own evidence not surviving contact: AO1's *"control run: 5 of 12"* was a control over the extracted rule and never over the call using it, and the fix could be reverted with the whole tree green. Its second half is the more transferable one: the reason no probe drove that call was a constraint borrowed from the suite, and the technique that gets round it (pi's own jiti) had been sitting in the probes directory since the thirteenth pass. §1 to §9 are a complete description of the machine assuming none of the twenty-three documents before it — §1 is seven ASCII panels, and three of them are new: **§1.2 is every name this stack carries and who spells it which way**, **§1.6 is the three resolution ladders side by side** (and the rule that separates them: report ambiguity when the caller cannot see what you picked), and **§1.7 is the published form against the accepted form**, which is the headline finding in one drawing. §2 is pi itself — where tool names come from and why they are not one case, what `JSON.parse` hands you, and pi's own four-line `getAgentDir()` down to the two details that are the whole of AO7. **§10.2 is the artefact: the identity ledger** — fifty-three rows, each naming the two values, the function that decides, and who supplied each side; §10.2.1 sorts them by **who minted the value** and finds every finding within one step of a boundary, with one instructive exception. §10.3 is the three ladders, §10.4 every fold in the stack and what each is for, §10.5 every prototype-reachable lookup. §11 has AO1–AO7 plus §11.8, two tests that pinned the wrong thing. The axis: **for every place this stack decides two values are the same, name the two values, name the function that decides, and find the pair that is equal-but-different or different-but-equal.** The headline is AO1 — an agent id is seventeen characters, eleven surfaces publish the first eight of it and four of those are read by the model, and `StopAgent` resolved it with an exact `Map.get`: 0 of 200 published ids resolved, and the refusal handed back more of them. AO5 is the prinny suite green against a staged build whose sources are not in the tree — measured live, 511 tests about a program nobody could produce from this checkout. AO6 is four lookups over `JSON.parse` output that answered for eight names nobody stored, one of them the outbound room gate whose docstring names prompt injection as the actor it exists for. §13.2 is the other half — fourteen things this axis checked and found already correct, with the reason. **§11.9 is AO8**, which this document first recorded as an open latent and then closed: `sameRepo` compared a realpath'd path against one that was not, and `git rev-parse --git-common-dir` answers relative in a main worktree and absolute in a linked one, so a worktree of the parent's own repository read as cross-repo. The reason given for leaving it — "the case that would prove it is not reachable on this box" — took four minutes with real git to disprove, and §13.1 keeps the mistake rather than the entry: "I cannot reach this" and "I cannot drive this" are different sentences. 1,369 → 1,424 → **1,434** tests, lint 115/115, 113 → **121** probes. **Start here for that subsystem.** |
| `design/subagents-loop-verifier-round-trips.md` | **The twenty-third pass**, superseded as the start-here document by `…-identity.md` and still the only account of the ROUND-TRIP ledger. §1 to §9 are a complete description of the machine assuming none of the twenty-two documents before it — §1 is seven ASCII panels, and two of them are new: **§1.2 is everything that leaves the heap** (every file, pipe, session entry, environment variable and in-memory buffer, with a ✘ on each one this pass found something in) and **§1.3 is the four kinds of round trip and what each one loses**; §1.6 draws the four config files and their four different answers to "what if I cannot parse it"; §1.7 draws the staged prinny runtime and its four readers. §2 is pi itself — how a session entry is written and walked back, why a `custom` entry is never context, and why the tool arguments an extension mutates are a `structuredClone`. **§10.2 is the artefact: the round-trip ledger** — thirty-eight rows, each with which of five gaps it crosses and what the reader does when the bytes are absent, malformed, stale or from another writer; §10.2.1 draws the seven findings by DISTANCE and finds **six of seven at distance zero, five of them literally adjacent to the correct version**. §10.5 is the switch table. §11 has AN1–AN7 plus §11.8, two tests that pinned the wrong thing. The axis: **for every value this stack puts outside its own heap, name the writer, the reader, and what the reader does when the bytes are not what the writer meant.** The headline is AN1 — one `catch` returning `{}` for both "there is no file" and "nobody could read this file", in two packages, where the next write REPLACES it; the prinny instance takes `permissionMode` from `all` to `off` and then saves that. AN2 is the staged sidecar runtime, which three readers and the launch script called "built" while it was a build from before AL3 — measured on this box, missing `connect.ts` entirely. AN5 is 41% of a real session file being loop state, two of every five entries byte-identical to the one before. §13.2 is the other half — eight things this axis checked and found already correct. 1,281 → 1,369 tests, 111 → 118 probes. |
| `design/subagents-loop-verifier-concurrency.md` | **The twenty-second pass**, superseded as the start-here document by `…-round-trips.md` and still the only account of the INTERLEAVING ledger. §1 to §9 are a complete description of the machine assuming none of the twenty-one documents before it — §1 is seven ASCII panels including the AWAIT MAP and, in §1.3, the two guarantees pi gives you and the five it does not; §2 is pi itself (tool calls are PARALLEL by default; the two compaction call sites and why only one is dangerous; what a session swap does to everything an extension is holding); §3 the event bus with `can a run start here?` against every row; §4–§9 the seven packages; §1.7 is the LOAD ORDER and the four behaviours that depend on it. **§10.2 is the artefact: the interleaving ledger** — thirty-five awaits, each with how long it can suspend for and what re-reads the world afterwards; §10.2.1 draws the six findings by DISTANCE and finds the opposite distribution to the pass before it. §11 has AM1–AM6 plus one correction. The axis: **for every `await` inside a handler, a settlement chain or a callback, name what ELSE can run at that point — then name what the code assumes has not changed by the time it resumes.** The headline is AM2, which closes a seven-pass-old bound: three senders ask whether somebody is compacting this session and all three could only see the two EXTENSIONS that compact, never pi. AM1 is a stop that read a handle assigned on the line after a 120-second await. AM3 is a teardown that disposed the session the VERIFIER was running in, so a good answer went back to the parent annotated as having failed. AM4 is the one continuation that survives a session swap, acting on the newly restored run with the previous session's handles. §13.2 is the other half of the pass — six things this axis checked and found already correct, with the reason. 1,222 → 1,281 tests, 106 → 111 probes. |
| `design/subagents-loop-verifier-lifetimes.md` | **The twenty-first pass**, superseded as the start-here document by `…-concurrency.md` and still the only account of the LIFETIME ledger. §1 to §9 are a complete description of the machine assuming none of the twenty documents before it — §1 is seven ASCII panels including the LIFETIME MAP and the four SCOPES, §2 is pi itself (including a table of what pi ends for you and what it does not), §3 the event bus and its four teardown events, §4–§9 the seven packages. **§10.5 is the artefact: the lifetime ledger** — forty-three constructs, each with its START, the one place that is its END, and the count of ways the work can finish; §10.5.1 draws the nine findings by DISTANCE (seven of nine are distance zero) and §10.5.2 names the four shapes a lifetime fails in. §11 has the nine findings AL1–AL9 plus three corrections. The axis: **for every construct with a beginning and an end, name the ONE place that ends it, then enumerate the paths that reach the end of the WORK without reaching the end of the THING.** The headline is the sidecar's connect loop, which retries a homeserver forever and built a Matrix client per attempt on one Olm crypto store, in a file whose own header says that store must never be shared between two running bots. 1,174 → 1,222 tests, 97 → 106 probes. |
| `design/subagents-loop-verifier-proxies.md` | **The twentieth pass**, superseded as the start-here document by `…-lifetimes.md` and still the only account of the PROXY ledger. §1 to §9 are a complete, self-contained account of the machine — §1 is the whole thing in six drawings, and **panel F is new: where the evidence of a delegation goes**, before and after this pass; §2 is **pi itself**, the section to read before changing anything, and §2.2 is the entry surface (`appendEntry` / `registerEntryRenderer`) and §2.3 is why a tool registered late is live for the next turn; §3 is the event bus, with the whole table, all nine orderings, and §3.3 on which emitters THREAD a handler's return and which discard it; §4–§9 each of the seven packages. Its question is **what is the test a proxy for: write the set down twice, once from the predicate's NAME and once from its CODE, and enumerate the difference.** Findings AK1–AK5, **all five fixed**, each with a regression test that fails when the fix is removed and a probe (`x1`, `x3`–`x6`) that prints BEFORE and NOW. **§10.5 is the proxy ledger** — seventeen predicates, each with the property it names, the test it runs and the set where the two differ — §10.5.1 draws the five findings by the DISTANCE between the two halves (four of five have both in the same file), and §10.5.2 names the three shapes a proxy fails in, each of which says what its fix has to be. AK5 is the loop's audit rung, which could not fire on the runs it was written for because `42 passed` counted as a change to the project. AK2 is the `dangerous` permission mode, whose help promises *"rm -rf, sudo, force push, curl|sh, and similar"* and which missed five of seven spellings of one `rm`. AK4 is a permission prompt that stayed answerable forever and reported `✅ Allowed` for a call that had already been blocked. AK1 is `registerTools` behind an `isConfigured()` read at load time — so the session that configures the channel is the session in which the model was never told that `[matrix]` text is untrusted input. AK3 is a JSON-RPC request read as a reply, with the answer for it already written eight lines below and unreachable. **§5.7 is the transcript work the operator asked for**: a subagent's own turns, in the session transcript the operator's turns go into, marked as a subagent's — with the property it rests on measured against pi's own `SessionManager` first (probe `x2`). §12 is the evidence (1,108 → 1,153 tests, 87 → 93 probes, lint 97/97, the *before* column measured before anything was written), §13.2 the negatives including the whole of `compaction-guard` for the fourteenth time, §13.3 the unwatched list. |
| `design/subagents-loop-verifier-authority.md` | **The nineteenth pass**, superseded as the start-here document by `…-proxies.md` and still the only account of the five ACTORS. §1 to §9 are a complete, self-contained account of the machine — §1 the whole thing in one drawing, opening with **the five actors** (a table no earlier document had); §2 **pi itself**, which is the section to read before changing anything, and §2.6 is `_tryExecuteExtensionCommand` and the context it hands over, which is where a Matrix sender's slash command lands; §3 the event bus, with two columns that were missing and **all nine** multi-handler orderings rather than the four that were interesting; §4–§9 each of the seven packages in full. Its question is **who is allowed to ask: name every actor that can reach a decision, not just the one the guard was written against.** Findings AJ1–AJ5, **all five fixed**, each with a regression test that fails when the fix is removed and a probe (`w1`–`w5`) that prints BEFORE and NOW. **§10.5 is the authority ledger** — every guarded surface against the five actors, with a ⚑ on every row where more than one arrives — and §10.5.1 draws the five findings by the actor each guard NAMES: four of the five are distance zero or one. AJ1 is `/stack`, whose own help says *“every mutation above is a user-only command on purpose”* while `MATRIX_ALLOWED` had `stack: null`; five subcommands had a confirmation that is a modal in the operator’s terminal saying nothing about who asked, and five had none at all. AJ2 reopens a decision — §11.4 of `…-controls.md` left the `loop` tool’s `check` open because *“the caller is already inside the trust boundary”*, and the caller of a tool is the model, whom `permissionMode` exists to say is not. AJ3 is the command a person approved not being the command that ran, because `rtk-pi`’s handler runs one position later on the same mutable object. AJ4 is the child’s own answer able to close the fence it is quoted in, in the judge’s prompt, where the defence already exists twice in `prinny-channel/src/inbound.ts`. AJ5 is the map itself: `tool_call`’s order was documented backwards and `browser-guard.ts` had no column, and `t5` — the standing scan written to keep that table honest — could see neither, because it was given the map’s own list. §10.3 states a rule for the first time: **fail open when the failure costs quality, fail closed when it costs a decision that belongs to a person.** §12 is the evidence (1,071 → 1,108 tests, 82 → 87 probes, lint 95/95, the *before* column measured before anything was written), §13.2 records the negatives including the whole of `compaction-guard`, §13.3 is the unwatched list, and **§13.4 is the one piece of work asked for and not built** — a subagent's turns in the session transcript. |
| `design/subagents-loop-verifier-promises.md` | **The eighteenth pass, and the previous self-contained account.** §1 to §9 are a complete, self-contained account of the machine — §1 the whole thing in one drawing, §2 **pi itself**, which is the section to read before changing anything (§2.5 is why AH1's fix widened this pass's AI1; §2.7 is `pi.exec`'s two missing facts, measured against the real function), §3 the event bus and the two things that are NOT on it, §4–§9 each of the six packages in full, each ending in its one-slot queues and what empties them. Its question is what the stack has already SAID: **quote the sentence — to a person, to a model, or to the next reader — and then find the path on which it is not true.** Findings AI1–AI5, **all five fixed**, each with a regression test that fails when the fix is removed and a probe (`v1`–`v5`) that prints BEFORE and NOW. **§10.5 is the promise ledger** — every sentence this stack says to somebody, quoted from the source, with who hears it, what keeps it and what makes it false — and §10.5.1 draws the five failures by DISTANCE: in four of them the promise and its undoing are in the same file, and in two of those the correct treatment of a *different* slot is on the screen at the same time. AI4 is the most serious: `forwardToMatrix` refuses to send when two rooms are live because *"guessing would send one person's conversation to another — worse than silence, and not undoable"*, and the `prinny` TOOL is the second route into the same `reply` and **guessed**, from `lastInbound` — a one-slot last-write-wins variable — while `renderInboundMessage` deliberately withholds `room_id` from the model, so the model could not correct it. AI1 is a finished background subagent's answer cleared out of the coordinator's batch set at `session_shutdown` in silence, with the drop report that exists for exactly that case unreachable — and AH1 had widened the window from 200 ms to five minutes. AI2 is a Matrix `/compact` promised *"as soon as it finishes"* to N senders and answered to one, and dropped at teardown four lines below a loop that denies every pending permission because *"the channel going away is not consent"*. AI3 is `steer()` answering `true` for a steer parked in a queue that `onSessionCreated` flushes, on a run that dies in the seconds before `onSessionCreated` exists — with `growBrief` having already put it in the brief the JUDGE checks against. AI5 is the seventeenth pass's own residue note (*"the remaining seven are script runners whose output is reported verbatim"*) being wrong about five of seven, two of them 600-second timeouts over an operation the same file calls "roughly 20 minutes". §14's transferable rules: **a deferral is a promise, and a promise has a second half**; **a fix that converts a drop into a wait converts a narrow window into a wide one — when you widen a window, list what crosses it**; **the best control is usually in the same file**; and **write down what you checked, so the next reader knows what to re-check rather than having to re-read.** §12 is the evidence (1,041 → 1,071 tests, 77 → 82 probes, lint 95/95, with the *before* column measured before anything was written), §13.2 records the negatives — including the whole of `pi-loop-mode`, which this axis clears — and §13.3 is the unwatched list in cheapest-first order. Its §10.5 (the promise ledger) and §10.5.1 (the same five by DISTANCE) are not restated by the nineteenth pass's document. |
| `design/subagents-loop-verifier-instances.md` | **The seventeenth pass, and the previous self-contained account.** Its §10.5 is the second-instance graph, which no later document restates. §1 to §9 are a complete, self-contained account of the machine — §1 the whole thing in one drawing, §2 **pi itself**, which is the section to read before changing anything (and §2.5 is what a run started *inside* a compaction actually costs, measured out of `agent.js`'s `createContextSnapshot`; §2.7 is `pi.exec`'s two missing facts), §3 the event bus, §4–§9 each package in full. Its question is the one the sixteenth pass left in its own handoff: **a rule that is right is applied where it was found — name every other place it belongs, from the code that COULD need it rather than from the code that already asks.** Findings AH1–AH6, **all six fixed**, each with a regression test that fails when the fix is removed and a probe (`u1`–`u5`) that prints BEFORE and NOW. **§10.5 is the second-instance graph** — every rule this stack has paid for, where it was written, and every other place it belongs, laid out by DISTANCE — and it is the artefact this pass exists to leave behind. AH1 is the residue the sixteenth pass wrote down and looked for in the future: `compactionInFlight()` had four readers and *"there is no test that a fifth would be noticed"*. The fifth already existed — `SpawnCoordinator.emitIndividualNudge`, the third sender through `sendCustomMessage`'s `triggerTurn` branch and **the only one with no second attempt**, because it runs once per record on a record whose slot is released and whose completion gate is open. AH2 closes §11.11, open since the twelfth pass: `parseJudgeVerdict` read `VERDICT: UNADDRESSED` as ADDRESSED with `unparsed: false`, so the answer went back to the parent model as `passed` **with no note at all** — and the decision that left it open named one fix (`\b`), correctly rejected it, and never asked whether it was the only one. AH3 is `killed`-before-`code` at three more `pi.exec` sites in the package whose own module header says the rule is "AA2 one package over", plus two in `stack.ts` where a wedged docker daemon reported every container "not running"; the fix is a **standing scan** rather than three edits. AH4 is the second spill directory, unbounded next to the one whose docstring names the unattended `/loop`. AH6 is AG2's own deferral deleting the directive it was meant to delay, because it parks it in the loop's ONE timer slot and `agent_end` clears that slot at its first line. §14's transferable rules: **W1–W6 is not a stage, it is the steady state, and only a LIST covers the instances behind you**; **two of six fixes are standing scans, and a scan that matches nothing passes**; and **a decision to leave something open is a claim that ages — write down which fix you considered, so the next reader can check whether it was the only one.** §12 is the evidence (1,018 → 1,041 tests, 72 → 77 probes, lint 91/91 → 95/95, with the *before* column measured before anything was written) and §13.3 is the unwatched list in cheapest-first order. Superseded as the start-here document by `…-identity.md`. |
| `design/subagents-loop-verifier-references.md` | **The sixteenth pass.** §1 to §9 are a complete, self-contained account of the machine — §1 the whole thing in one drawing (the two entry points, the three nested units of a turn, the five extensions, a delegation, a Matrix exchange), §2 **pi itself**, which is the section to read before changing anything (the two entry points as a table, and every row of it is a previous finding), §3 the event bus **rebuilt from the source**, §4–§9 each package in full. Its question is what the stack NAMES: **name the flag, the tool, the entry point, the surface or the sibling rule that a decision or a sentence points at, and then go and read it.** Findings AG1–AG6, **all six fixed**, each with a regression test that fails when the fix is removed and a probe (`t1`–`t5`) rewritten afterwards to print BEFORE and NOW. §11 has the fix and the control-run failing count for each; §12.1.1 is the table of what each one cost. Five of the six are a pointer that was never followed, and in every one the thing pointed at already exists and already works. AG2 and AG3 are the same moment from two extensions: pi's *"cannot prompt while compacting"* refusal lives on `prompt()` alone, and both the loop's own next iteration (`sendCustomMessage` → `_runAgentPrompt`, which checks nothing) and prinny's empty-turn continuation are delivered **into a compaction that is already running** — while `compactionInFlight()`, the lock the fifteenth pass built, says who is compacting. Four call sites could read it; two do. AG1 is AF5 at the other end of its own distribution: `briefForCheck` reserves half the judge's budget for the accumulated follow-ups and never spends the rest on them, so on a one-line brief steered four times the judge is shown **one steer of four and 1,019 characters of a 1,500-character budget go unused** — and every AF5 test uses the one shape where the two splits agree. AG4 is the event-bus map itself: five documents draw `pi-subagents-lite` on three events it does not handle and omit the two it does, and the same document's own §1.C summary contradicts it. AG5 and AG6 are two operator-facing sentences that name the wrong thing — "still busy" for a refused *stop*, which can only mean already finished, and "Read it with AgentStatus" for an answer `AgentStatus` does not print. §14's transferable rules: **the thing you named is a file you can open**; **a fix has a shape, and the shape has more than one instance**; and **a reserve is not a cap — say what happens to the room nobody used.** §12 is the evidence (991 → 1,018 tests, 67 → 72 probes, lint clean, with the *before* column measured before anything was written) and §13.3 is the unwatched list in cheapest-first order. Superseded as the starting point by `…-instances.md`, whose §1–§9 are a self-contained account of the same machine and whose §10.5 answers the residue this pass recorded. |
| `design/subagents-loop-verifier-omissions.md` | **The fifteenth pass, and the one to read next** — its §2 is the refusal ledger, the artefact that pass exists to leave behind. It asks about the places the stack decides NOT to act: **name the guard that declines, name what it was holding when it declined, and say who owns that thing afterwards.** §2 is the ledger that produced — forty-five refusals, each with the object it was holding and where that object went; §2.1 draws the distance between a refusal and the thing it dropped. Every refusal in the stack is CORRECT and none is reversed here; six of them had nowhere to put down what they were holding. Findings AF1–AF6. AF1: `forwardToMatrix` refuses to send when two rooms are live, because with two there is no way to tell whose answer it is — and eight lines later the same handler retires every live room, so **two people, two questions, zero answers, zero notices**, with the entries deleted so the undelivered sweep could not report it either. AF6: the output cap began `if (event.isError) return`, and pi's bash tool throws the WHOLE output on a non-zero exit (up to 50 KB) — so the cap **exempted the one shape an unattended `/loop` produces every iteration**, a test suite that is still red. AF3: five exits of `agent_end` charged the whole ladder and then dropped the directive they were charged for, which is V4's own finding at the five sites V4's comment named but did not visit. AF2: `abort()`, `clear()` and `steer()` each answer with a boolean and five of six call sites discarded it — "Cleared 1a2b3c4d" about a record that is still there, and a viewer steer that said nothing at all. AF4 and AF5 are the same defect in two packages: `slice(-N)` over a newest-first array, so `AgentStatus` listed the six OLDEST agents and called the newest "older"; and a head cut over a brief that grows at the tail, so the judge never saw the steer the answer was about. §14's transferable rules: **a refusal is half a decision — the other half is the object**, and **a bound is a refusal with a rule in it, so check the rule against what the caller actually hands over.** §10.6–§10.8 are the three items closed after the findings: the cross-extension compaction lock (§11.12, the fourteenth pass's homework), the verification log (`~/.pi/agent/subagent-verify.jsonl` — #1 on the unwatched list since the fourth pass), and the three silent drops of a background result (§11.1). Superseded as the starting point by `…-references.md`, whose §1–§9 are a self-contained account of the same machine. |
| `design/subagents-loop-verifier-claims.md` | **The current account of subagents, the loop and the verifier.** The fourteenth pass, and the one that asks about the machine's account of ITSELF: **name the flag, name the fact it stands for, and then name everything that can make the fact false, and say whether the flag hears about it.** A flag is any value the stack keeps about its own state and later acts on — `state.active`, `lifecycle.status`, `entry.live`, `retrying`, `params.model`, `pendingCompaction`. §2 is the ledger that produced: twenty-four of them, organised by FALSIFIER rather than by writer, plus §2.1's claims graph, which draws the DISTANCE between each flag's writer and the thing that invalidates it. Five had a falsifier that never wrote them. Findings AE1–AE7. AE1: the loop's operator-abort rung set `status = "paused"` — a display field nothing branches on — and left `state.active` true, which is what all thirteen handlers test at their first line, so **"Loop paused. Use /loop resume to continue" was a sentence about a loop that then resumed itself on the next turn from any source**, including the operator answering the notice. AE3: `awaitingReply` is keyed by ROOM and every inbound message replaced the entry, so a `/compact` from the same person waiting for an answer **took the answer with it**, silently, and suppressed the sweep that would have said so. AE4: `retrying` is claimed on a call that cannot report failure, and it is what keeps a room live — so a continuation that never happened left a stranger's room live and **the operator's next terminal answer was forwarded to it**. AE2: AD3's deferred `/compact` aborts the continuation the same handler starts one line earlier. AE6: AD1 made `params.model` load-bearing, and the two ends resolve it against different names and different registries. AE5: three more readers of `lifecycle.status`, including the `AgentStatus` tool, which could elide the one agent holding the llama slot. AE7 is a walk that crosses a boundary its sibling stops at. §14's transferable rules: **a flag is a cache, and every cache has an invalidation problem**, and **"nothing is in flight here" is a claim about the future that the code below it can falsify.** Superseded as the starting point by `…-omissions.md`, whose §1 extends its §1. |
| `design/subagents-loop-verifier-controls.md` | The thirteenth pass, and the mirror of the twelfth: that one followed the ANSWERS outward (*name the reader*), this one follows the INSTRUCTIONS inward — **name the mechanism, and say what happens to the instruction it was given.** §2 is the ledger that question produced: twenty-eight instructions — a model override, a `--check`, a `StopAgent` call, a permission mode, an allow-list entry, an env var — with where each is set, where it is resolved, which mechanism is supposed to obey it, and whether that mechanism ever sees it. Five were not obeyed. Findings AD1–AD7. AD1: the six-level subagent model precedence was resolved by `modelFor()`, injected onto the tool call by `toolCallListener`, rendered next to the call as `▸ Explore (qwen3-4b)`, listed as the effective model in `/agents` — and dropped by one `undefined` in `executeAgentTool`, so **every child ran on the parent's model**, frontmatter included, and every child held the parent's concurrency slot. AD2: T5's stop was fixed at `stopAgent()` and the `StopAgent` TOOL's own precondition returned before the manager was ever asked, telling the model an agent was "already completed" while a judge held the single llama slot. AD3: AC5 made `/compact` from Matrix real, and pi's `compact()` begins `await this.abort()` — so a remote message cancelled the operator's turn and **paused an unattended loop, recorded as `Turn aborted by operator`**. AD4 is the command receipt that claims success it cannot observe; AD5 is `/agents` in none of the three routing tables; AD6 is `--check`, the one argument on the Matrix-allowed surface that runs a shell command through `pi.exec` and therefore past the permission relay, rtk's gate and the output cap alike; AD7 is `--rescue-model`, the same model switch `--model` is refused for. §14's transferable rules: **a test that pins an ABSENCE is a test of a premise** — AD1's fix required deleting the twelfth pass's own regression test — and **resolution is not application; only one of the two is worth a test.** Superseded as the starting point by `…-claims.md`, whose §2 is the successor to its §2. |
| `design/subagents-loop-verifier-deliveries.md` | The twelfth pass, and the first that reads the stack as a delivery network: for everything that produces an answer it asks **who receives this, and what do they see when nobody does?** §8 is the ledger that question produced — fourteen carriers, their readers, and what each reader sees when the carrier drops the parcel; eleven of the fourteen are `void`, fire-and-forget, or a `catch`. Findings AC1–AC5. AC1: AA4's edit deleted `const ctx = getSessionCtx()` along with the ternary that used it and left three readers below it, so `emitIndividualNudge` threw `ReferenceError` three lines before `pi.sendMessage` — **every background subagent's answer, and every continuation's, stopped reaching the parent model** from the tenth pass to this one, reported through a `catch` written for a different failure into a UI method that is `() => {}` headless. AC2: `/loop resume` carries the check's verdict and error streak across from the run that ended. AC3: a bash `EXIT` trap is a slot, not a stack, so a check that cleans up after itself removed AB1's completion marker and read as one the OOM killer reaped. AC4 and AC5 are the two Matrix findings: a delivery report about a message that was never a delivery, and `/compact` allow-listed, advertised, and undispatchable. §14's transferable rule: **a test that cannot execute the code it protects is a test of the diff** — a source pin catches a revert, not a deletion three lines away. Superseded as the starting point by `…-controls.md`, whose §2 is the mirror of its §8. |
| `design/subagents-loop-verifier-signals.md` | The eleventh pass, and the first that draws the whole stack: §1 has all five extensions on one page, including `prinny-channel` and `rtk-pi`, which the first ten passes never opened. §6 and §7 are the first full accounts of those two; §8 is the **host-call ledger for both**, which is the homework the tenth pass left. Findings AB1–AB4. AB1: `result.killed` is set only inside pi's own `killProcess()`, so it means "did pi stop this" rather than "did this finish" — a goal check reaped by the OOM killer comes back exit code **0** and reads as a check that PASSED, which is the single terminating condition an `--until-done` run has. AB2: `pi.sendUserMessage` returns void, pi `.catch`es the rejection into `emitError`, and `emitError` has no listener outside a TUI — so a Matrix message pi refused was dropped with neither the sender nor the operator told. AB3 and AB4 are the same question asked of `pi.exec` in the one package nobody had read, and of an `AbortSignal` whose event had already been dispatched. §8.3 is the **five**-surface checklist: the tenth pass's four, plus *when* a host's answer can be read and how long it stays true, which is what all four of these findings are about. Superseded as the starting point by `…-deliveries.md`, which extends its §1 and its §8.3. |
| `design/subagents-loop-verifier-hosts.md` | The tenth pass, and the ninth's completion. It is the document that first drew `compaction-guard`, which the first nine passes never opened: the ninth found three bad calls by reading the three it happened to be looking at, so this one read **all thirty-one host calls** and §7 is the ledger — every pi API the code calls, pi's implementation, and where the value ends up. Findings AA1–AA4. AA1: `before_agent_start` is emitted from ONE place in pi, `AgentSession.prompt()`, and the loop drives every turn through the other entry point — so the handler that puts the loop's rules in the system prompt never ran on a loop turn, and what it returned when an operator typed outlived the run on `agent.state.systemPrompt`. AA2: `pi.exec` never rejects, and a check killed on its own timeout comes back with exit code **0** — so a deadlocked goal check reads as a check that PASSED and completes an `--until-done` run. AA3 and AA4 are a premise and a dead branch. The same pass then emptied the note list every prior pass had been adding to — §9 is now a record of what became of each entry rather than a backlog — and closed T5 (a verification was uninterruptible) and `prinny-channel`'s W1 shape. §1 is the master drawing (the two entry points, the three nested units of a turn, where the system prompt comes from per turn, the two compaction call sites, the four packages and the order they load in); §5 is `compaction-guard`; §7.5 is the four-surface checklist the whole series has been converging on. Superseded as the starting point by `…-signals.md`, which extends its §1 and its §7.5. |
| `design/subagents-loop-verifier-answers.md` | The ninth pass, and the twin of the eighth: that one found the harness not replaying what pi does with a handler's RETURN value; this one found three calls whose ARGUMENTS pi routed somewhere the call site's own comment did not describe — a delivery mode with one drain site (`AgentSession.prompt()`, the operator-typed path), a `session.steer()` that restarts an agent loop that had already stopped, and an index into an array pi rebuilds on every compaction. Findings Z1–Z4, all four proved by probe, **all four fixed**. Its §1 is every route by which a message reaches a model and who drains each queue; §3.2 is what a subagent RUN says as opposed to what its last message says; §8.1 is the complete table of what pi does with every extension handler's return value, which is the check the eighth pass asked for. Its §12 is the instruction the tenth pass carried out; its §1 note that `Agent.continue()` drains the steering queue is incomplete — it drains follow-ups too. |
| `design/subagents-loop-verifier-turns.md` | The eighth pass, and the first that is about the EVIDENCE rather than the code: `_host.mjs` did not replay pi's in-place `message_end` replacement, and that is the one thing the module under test uses to change what a later handler sees — so a whole rule of `detectStuck` had been dead in every real session since the fork while every probe showed it working. Findings X1–X5 and Y1, all six proved by probe, **all six fixed**, each with a regression test that fails when the fix is removed. Its §2.2 is one turn read NINE ways, §2.3 the three per-turn buffers and why there are three, §2.5 the eight stuck rules against what each one reads, §2.8 what every lifecycle transition puts back, and §3.2 who may touch a record while its verifier is running. Its §2.2 `runTurnLoop` row is what Z1 corrects, and its §7 note on the sanitizer's idempotence is what Z3 corrects. |
| `design/subagents-loop-verifier-readers.md` | The seventh pass, and the first that is about the earlier passes themselves: every finding is the **second reader** of a fact a previous pass fixed at one site. Findings W1–W6, all six proved by probe, **all six fixed**, each with a regression test that fails when the fix is removed and a probe that prints BEFORE and NOW. §10 has what shipped and the control-run failing count for each. Its §1–§5 redraw the whole machine — the message/turn distinction, `agent_end`'s ladder, `steer()`'s three branches, the turn ceiling as a state machine, the verifier's round loop and note vocabulary, the delegation timeline. (Its "start here" marker is stale — six passes have superseded it; the current one is `…-controls.md`.) |
| `design/subagents-loop-verifier-shapes.md` | The sixth pass, drawn as data rather than control flow: the whole machine on one page, one assistant turn read seven ways, `LoopState`'s four reset paths side by side, `RunResult`'s five fields and who reads which, the `agent_end` ladder, the stuck ladder's three rungs and their guards, and the verifier's rounds. Plus the sixth pass: findings V1–V8, seven proved by probe, **all eight fixed**, each with a regression test that fails when the fix is removed and a probe that prints BEFORE and NOW. §10 has what shipped and the control-run failing count for each. Its §2 drawing is what W1 extends and its §4.3 turn-ceiling table is what W4 completes. |
| `design/subagents-loop-verifier-units.md` | The fifth pass: findings U1–U9, eight proved by probe, **all nine fixed**, with the control-run failing count for each. Its §9 reference sections — the record's life, concurrency, the turn ceiling, the context ladder, the handoff summary, the compaction guard, what it costs on the wire — are the detail the sixth-pass document does not restate. |
| `design/subagents-loop-verifier-surfaces.md` | The fourth pass: findings S1–S10, six proved by probe, **all ten fixed**, with the control-run failing count for each. Its §1 map and §3–§8 detail still hold and are not restated in the fifth-pass document. |
| `design/subagents-loop-verifier-mechanics.md` | The third pass: the mechanics, the seams between the three packages, findings T1–T9 (four fixed). Still the best account of pi's agent loop and the steering-drain order; its §12 was re-checked by the fourth pass and holds. |
| `design/subagents-loop-verifier-evaluation.md` | The second audit (F1–F11), with the reproductions. Superseded on no point; kept because the reproduction is the evidence. |
| `design/subagents-loop-verifier-anatomy.md` | The first audit and the **design account** — why each piece is shaped the way it is (B1–B8). Carries two header notes and five inline corrections from the later passes. |
| `testing/subagents-loop-verifier.md` | Hand-testing script for subagents, the `loop` tool and the verifier: a prompt per behaviour and what counts as evidence, including the `--mode json` recipes for the things that are invisible in the TUI, and the paths that have never fired live. **§AI is the newest section** — the twenty-fourth pass (AO1–AO9), nine items, four of them terminal-only. It was written 2026-08-23 because two documents referenced it in three places and it did not exist. **§AI.1 is the one to read**: `AgentStatus` then `StopAgent` with the eight characters it printed, run live with a real control run — and the control run is what found **AO9**, that AO1's fix could be reverted with all 1,434 tests and 121 probes staying green. It also records a general fact about headless testing: a `pi -p` run prints no slash-command result and writes no session file, so no slash command's operator-facing sentence can be hand-tested that way. Below it, **section AD**: §AD.1 is `/stack restart llama` from a phone (AJ1), §AD.2 is a goal check the model wrote and is the first item in the whole script that tests a REFUSAL a person has to answer (AJ2), and §AD.3 is the command a person approved against the command that ran (AJ3). Below it, **§AC.1 is the most serious unrun thing on the list** — two rooms live in one turn plus a `prinny` tool call, which is AI4 end to end and needs only a Matrix account. **Section B is otherwise the one to run first** — it is the test that would have caught AC1 — and **section I has still never been run**, and neither have §J (steering a running subagent), §K/§K.2 (a verifying record in `/agents`, and stopping its check now that T5 is closed), §L (a child that compacts), §M and **§M.2** (one loop turn watched — the cheapest run on the list, and where three HIGH findings across two passes sit; §M.2 is the goal check that is KILLED rather than slow), §N (a Matrix exchange with a background subagent running) **§O** (a Matrix message sent while pi is compacting, and `/compact` sent from Matrix), **§M.3** (a check that sets its own EXIT trap) or **§P** (a background subagent's result, end to end — the AC1 run). **§U is the new one to run first** — press Esc on a loop turn, then type a question: one keypress and one sentence, no subagent, no verifier, no Matrix account, and it is the AE1 run. §V and §W are the two Matrix sequences the fourteenth pass added. |
| `testing/probes/` | The executable reproductions behind the audits — real modules, stub hosts, no model. Eighty-two of them, in sixteen series — the sixteenth (`v1`–`v5`) is the promise pass: `v4` is the first probe to drive a real REGISTERED TOOL (prinny's) as well as the extension around it, and prints which room the one-slot `lastInbound` was pointing at; `v3` measures the subagent BUILD WINDOW on a real spawn; `v5` measures pi's own `execCommand` four ways at probe time rather than pinning it — the fifteenth (`u1`–`u5`) adds the second standing scan in the tree (`tests/exec-verdicts.test.ts`, greps every `pi.exec` verdict) and the first probe to drive the real `SpawnCoordinator` — the fourteenth (`t1`–`t5`) is the first to drive TWO real extensions against each other for something other than a compaction collision, and `t5` is the first standing check rather than a reproduction: it re-derives the event-bus table from the source and diffs it against any design document given as an argument. `_host.mjs` replays pi's in-place `message_end` replacement — read the note about that before trusting any probe that touches degenerate text. Its README says what each prints now and what it printed before the fix, and its eleventh-pass addendum is the other half of the eighth pass's lesson: a harness must be able to PRODUCE every distinct value the host can return, not only replay what the host does with one — its twelfth-pass addendum takes that one step further, to the tests — a fix whose test cannot EXECUTE the function it changed is pinned against editing, not against breaking — and its thirteenth-pass addendum takes it one step further still: a test that asserts an ABSENCE cannot be wrong about whether the text is there, only about why it should not be, so it must also assert what supplies the value instead — and its fourteenth-pass addendum is about the probes themselves: a probe that shares module-global state between its own scenarios has an unstated precondition, which is how `r3`'s leak scenario passed for the wrong reason until the four blocks were split into four processes. `_sidecar.mjs` is new and is not a probe: it is a stand-in for the prinny sidecar that a probe can DRIVE, which is what lets `r3` deliver a second Matrix message at a moment it chooses and read back exactly what the extension said. |
| `design/decisions.md` | Every design decision, in date order: model and quant choice, llama.cpp and forge flags, environment traps that cost real time, the settings review against other public Qwen3.6 rigs, the 2026-08-12/13 entries — the move to pi-only, MCP-as-a-CLI, the KV-quantization and memory findings, why headroom was evaluated and removed, the `/stack` extension, the Fable-Fusion evaluation, and the audit corrections — the 2026-08-15 entry, the migration to Qwen3.8-27B and the removal of the eval harness, and the 2026-08-16 entry, adopting rtk for bash-output filtering and the three research-pass findings that measurement overturned, and the eight audit passes over subagents/loop/verifier that close the file (L3895 second, L4049 third, L4204 fourth, L4323 fifth, L4493 sixth, L4649 seventh, the eighth at L4812, the ninth at L4922, and the tenth at the end). It also carries the **Still open** list at the end, which is the successor to the old root-level `HANDOFF.md`. |

Two conventions this directory follows, both worth keeping:

**Dates and observed values stay in.** Every claim records when it was verified
and against what, so a future reader can tell what has gone stale rather than
guessing. Superseded passages are marked as superseded rather than deleted —
the log is a record of what was believed and why, not a description of the
current tree. `README.md` is where the current tree is described.

**Unverified claims are labelled as unverified.** `THINK_LANG=zh` is on while
resting on a community claim, and says so, and names the harness that would
settle it (`scripts/ab-think-lang.sh`). Where a claim has since been measured —
the KV cache, the load mode, headroom's savings — the entry carries the number
and the method, not a conclusion on its own.

**Deleted history stays deleted, and stays described.** The scored eval harness
and its committed scorecard were removed on 2026-08-15. The entries that
recorded what it measured, and what it got wrong, are still here — a measurement
this repo has stopped taking is exactly the kind of thing a future reader will
otherwise re-invent.
